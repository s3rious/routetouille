{
  "name": "routetouille-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "turbo run format",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.13.0"
  }
}
